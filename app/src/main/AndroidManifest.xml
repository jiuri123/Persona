<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 读取外部存储权限，用于从相册选择图片 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <!-- Android 13及以上需要的读取媒体图片权限 -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/icon_persona"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_persona"
        android:supportsRtl="true"
        android:theme="@style/Theme.Demo">
        <activity
            android:name=".activity.OtherPersonaChatActivity"
            android:exported="false" />
        <activity
            android:name=".activity.UserPersonaChatActivity"
            android:exported="false" />
        <activity
            android:name=".activity.UserPersonaCreateActivity"
            android:exported="false" />
        <activity
            android:name=".activity.UserPostCreateActivity"
            android:exported="false" />
        <activity
            android:name=".activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>